export type Tab='today'|'workout'|'nutrition'|'progress'|'coach';const items:{id:Tab;label:string;icon:string}[]=[{id:'today',label:'TODAY',icon:'⌂'},{id:'workout',label:'WORKOUT',icon:'⚡'},{id:'nutrition',label:'NUTRITION',icon:'◉'},{id:'progress',label:'PROGRESS',icon:'▥'},{id:'coach',label:'AI COACH',icon:'◌'}];export function BottomNav({tab,onChange}:{tab:Tab;onChange:(t:Tab)=>void}){return <nav className="bottomNav">{items.map(x=><button key={x.id} className={tab===x.id?'active':''} onClick={()=>onChange(x.id)}><span>{x.icon}</span><small>{x.label}</small></button>)}</nav>}
